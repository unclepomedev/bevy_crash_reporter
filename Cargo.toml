[package]
name = "bevy_crash_capture"
version = "0.0.5"
edition = "2024"
description = "Captures panics and native crashes in Bevy games; you decide where reports go."
license = "MIT OR Apache-2.0"
repository = "https://github.com/unclepomedev/bevy_crash_capture"
readme = "README.md"
keywords = ["bevy", "monitoring"]
categories = ["game-development"]

[dependencies]
minidumper-child = "0.5"
bevy_app = { version = "0.19", default-features = false, features = ["std"] }
ureq = { version = "3", features = ["json"], optional = true }
serde = { version = "1", features = ["derive"], optional = true }
rfd = { version = "0.17", optional = true }
tracing = { version = "0.1", default-features = false, features = ["std"], optional = true }
tracing-subscriber = { version = "0.3", default-features = false, features = ["registry", "std"], optional = true }

[dev-dependencies]
tempfile = "3"

[features]
default = []
github-issues = ["dep:ureq", "dep:serde"]
recent-logs = ["dep:tracing", "dep:tracing-subscriber"]
confirm-dialog = ["github-issues", "dep:rfd"]

[[bin]]
name = "bevy_crash_trigger"
path = "tests/fixtures/bevy_crash_trigger/main.rs"
test = false
doc = false
